services:
  orders-api:
    build: .
    environment:
      APP_ENV: production
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 5s
      timeout: 2s
      retries: 3
